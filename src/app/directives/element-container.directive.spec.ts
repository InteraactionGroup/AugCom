import { ElementContainerDirective } from './element-container.directive';

describe('ElementContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementContainerDirective();
    expect(directive).toBeTruthy();
  });
});
